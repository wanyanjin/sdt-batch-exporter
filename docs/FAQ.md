# 常见问题

## 是否支持寿命拟合？

不支持。当前版本聚焦预览与导出。

## 为什么某些文件不能导出 intensity？

time axis 必须能被安全推断；如果结果歧义，程序会跳过 intensity 导出以保护数据完整性。

## PNG 可以用于定量分析吗？

不可以。PNG 只是显示渲染图，定量分析请使用 Zarr 或 CSV/TXT。

## 为什么仓库里没有示例 `.sdt` 文件？

真实实验数据不包含在仓库中。
